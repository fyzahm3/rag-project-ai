# Pinned to exact versions for reproducible builds. Bump deliberately (and re-run the
# test suite + a security audit, e.g. `pip-audit`) rather than letting these float.

# Core API
fastapi==0.111.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9
httpx==0.27.0

# Config & validation
pydantic==2.7.0
pydantic-settings==2.3.0

# LLM & structured output orchestration
openai==1.35.0
instructor==1.4.0

# Vector store & sparse retrieval
chromadb==0.5.3
rank-bm25==0.2.2

# ML runtime (CPU wheels are pre-installed in Dockerfile via the PyTorch CPU index)
torch==2.2.0
sentence-transformers==3.0.0
transformers==4.41.0

# Document parsing
pymupdf==1.24.0
beautifulsoup4==4.12.0
Markdown==3.6

# Numeric
numpy==1.26.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.0
