# Extra dependencies for local (tray daemon) mode only — not needed by, and not
# installed into, the server/Docker deployment. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-local.txt
#
# tkinter (the search window) ships with the standard CPython installer on macOS and
# Windows, so it isn't listed here — nothing to pip-install for it on those platforms.

watchdog==4.0.1
pystray==0.19.5
Pillow==10.3.0
PyYAML==6.0.1

# Generation: Ollama itself is a separate install (https://ollama.com), not a pip
# package — the local daemon talks to it over HTTP via Ollama's OpenAI-compatible
# endpoint, reusing the `openai` package already in requirements.txt.
